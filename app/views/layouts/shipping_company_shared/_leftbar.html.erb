<div id="layoutSidenav_nav">
    <nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
        <div class="sb-sidenav-menu">
            <div class="nav">
                <div class="sb-sidenav-menu-heading">Dashboard</div>
                <%= link_to shipping_company_root_path, class:"nav-link" do %>
                    <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
                    Dashboard
                <% end %>
                <%= link_to shipping_company_carriers_path, class:"nav-link" do %>
                    <div class="sb-nav-link-icon"><i class="fas fa-truck"></i></div>
                    Veículos
                <% end %>
                <%= link_to shipping_company_prices_path, class:"nav-link" do %>
                    <div class="sb-nav-link-icon"><i class="fas fa-tags"></i></div>
                    Preços
                <% end %>
            </div>
        </div>
        <div class="sb-sidenav-footer">
            <% if user_signed_in? %>
                <div class="small">Logado como:</div>
                <%= current_user.email %>
            <% else %>
                <div class="small">Usuário não autenticado</div>
            <% end %>
        </div>
    </nav>
</div>